{% extends 'base.html.twig' %}

{% set title = (vote is null) ? 'Ajouter un lien' : 'Modifier un lien' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
 <div class="wrapper">
    <div class="padding-10">
        {{ form_start(form) }}
        <div class="block">
            <div class="block-title">
                <h2>{{ title }}</h2>
                <span class="tools">
                    <a href="{{ path('admin_levels') }}" class="btn btn-secondary btn-ico" title="Retour à la liste des liens"><i class="fas fa-chevron-left"></i></a>
                </span>
            </div>
            <div class="block-body">
                <div class="form-group">
                    {{ form_row(form.title) }}
                </div>
                {{ form_row(form.content) }}
                <div class="form-group">
                    {{ form_row(form.startAt) }}
                    {{ form_row(form.endAt) }}
                </div>
                <b>{{ form_label(form.voteIssues) }}</b>
                    <ul class="collection_container {{ form.voteIssues.vars.name }}"
                    data-index="{{ form.voteIssues|length > 0 ? form.voteIssues|last.vars.value|length + 1 : 0 }}" 
                    data-prototype="{{ form_widget(form.voteIssues.vars.prototype)|e('html_attr') }}">
                        {% for item in form.voteIssues %}
                            <li>{{ form_widget(item) }}</li>
                        {% endfor %}
                    </ul>
                    <div>
                        <button type="button" class="btn btn-success add_item_link" data-collection-holder-class="{{ form.voteIssues.vars.name }}">
                        <i class="fas fa-plus-circle"></i> Ajouter une question
                        </button>
                    </div>
                </div>
            <div class="block-footer">
                {{ form_widget(form.save) }}
            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
