<div class="container">{{ current.content | raw}}</div>

<p>{{ user.lastLicence.amount['str']|raw }}</p>
<br>
<p>{{ user.health.isMedicalCertificateRequired }}</p>
<br>

<a class="btn btn-primary btn-xxl" href="{{ path('registration_file', {'user': user.id }) }}">Télécharger le dossier</a>
