<div class="container">{{ current.content | raw}}</div>

<p>{{ membership_fee_service.amount(user) }}</p>
<br>
<p>{{ user.isMedicalCertificateRequired(licence_service.currentSeason) }}</p>
<br>

<a class="btn btn-primary btn-xxxl" href="{{ path('registration_file', {'user': user_entity.id }) }}">VALIDER VOTRE INSCRIPTION<br>et télécharger le dossier</a>
