<turbo-frame id="crontab" class="w-100 flex-auto">
    <a href="{{ path('admin_crontab_logs')}}" data-turbo-frame="_top">
        <div class="dashboard-item-body">
            <div class="flex gap-4 bg-slate-300 px-3 py-1">
                {% include "components/_badge.html.twig" with {badge: cronTab.badge} %}
                <div> Dernière execution de la tâche planifiée : {{ cronTab.label }}</div>
            </div>
        </div>
    </a>
</turbo-frame>
