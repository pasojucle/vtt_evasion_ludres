{% extends 'base_admin.html.twig' %}

{% block title %}Calendrier des activités{% endblock %}

{% block body %}
    <div class="p-4 lg:p-6">
        <div class="flex mt-4">
            <div>
                <h1 class="font-bold text-3xl">Programme des activités</span></h1>
                <div class="text-muted mt-1">Administration des activités : création, modification.</div>
            </div>
            <div class="ml-auto">
                <div class="flex flex-col lg:flex-row items-center rounded-sm divide-y lg:divide-y-0 lg:divide-x divide-white">
                    {% include 'component/_dropdown.html.twig' with {dropdown: list.settings} %}
                </div>
            </div>
        </div>
        {% include('activity/admin/_frame_list.html.twig') %}
    </div>
{% endblock %}

