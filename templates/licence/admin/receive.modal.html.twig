{% extends 'base_modal_stimulus.html.twig' %}
{% set type = 'success' %}
{% block modal_title %}Inscription de {{ fullname }}{% endblock %}
{% block body %}
    <p>{{ message }}</p>
    <br>
    {% if form.licenceNumber is defined %}
        {{ form_row(form.licenceNumber) }}
    {% endif %}
    {% if form.medicalCertificateDate is defined %}
        {{ form_row(form.medicalCertificateDate) }}
    {% endif %}
{% endblock %}
{% block action %}
    <button 
        type="submit"
        class="flex justify-center text-white bg-{{ type}}"
    >
    <div class="flex gap-3 items-center ">
        {{ ux_icon('lucide:check-check', {class: 'size-6'}) }} 
        <div>Réceptionner</div>
    </div>
    </button>
{% endblock %}
