<div>
    <h1 class="font-bold text-3xl">{{ header.title }}</h1>
    <div class="flex flex-col gap-1 mt-1">
        <div class="flex items-center gap-2 text-muted">
            {{ header.period }}
        </div>
    </div>
</div>